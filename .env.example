# Lexicon: copy to .env and fill in. Do not commit .env.

# --- Primary user (you, the note-taker) ---
# Required for Self-evaluation and retrospectives: meeting notes include "What I did well / could improve."
# Without this, the system doesn't know who "I" is. Init syncs it to Metadata/User.md.
LEXICON_USER_NAME=

# --- Fireflies (per account) ---
# Use the same account name in env and in folder Transcripts/Fireflies/<account>/
# Example: accounts "personal" and "work"

# Account: personal
FIREFLIES_API_KEY_personal=
EMAIL_personal=

# Account: work (or company, kite, etc.)
FIREFLIES_API_KEY_work=
EMAIL_work=

# Optional: override where to save transcripts (default: Transcripts/Fireflies/<account>/)
# OUTPUT_DIR_personal=
# OUTPUT_DIR_work=

# --- Areas ---
# Areas are folder names you choose: e.g. work, personal, career, general.
# They appear under Meetings/<Area>/, People/<Area>/, Memory/<Area>/, and Transcripts/Manual/<Area>/.
# No env var needed; just create folders when you start using an area.
