# Lexicon: copy to .env and fill in. Do not commit .env.

# --- Primary user (you, the note-taker) ---
# Required for Self-evaluation and retrospectives: meeting notes include "What I did well / could improve."
# Without this, the system doesn't know who "I" is. Init syncs it to Metadata/User.md.
LEXICON_USER_NAME=

# --- Fireflies (per account) ---
# Default account is "personal". Use company name or another label for a second account (e.g. "acme").
# Project = Lexicon context for notes/memory (e.g. personal, company, career, general). If account name is not the project (e.g. "acme"), set PROJECT_acme=company so notes go to Meetings/company/.

# Account: personal (default)
FIREFLIES_API_KEY_personal=
EMAIL_personal=
PROJECT_personal=personal

# Account: company (or acme, etc.)
# FIREFLIES_API_KEY_acme=
# EMAIL_acme=
# PROJECT_acme=company

# --- Projects ---
# Projects: personal, company, career, friendship, general (Meetings/<Project>/, People/<Project>/, Memory/<Project>/).
# Full list and descriptions: Metadata/project_registry.md
# Fireflies: PROJECT_<account> sets the *default* project written into transcript frontmatter.
# The summarize step may override it if the meeting content belongs to a different project.
# Manual: set project in transcript frontmatter only (folder is Transcripts/Manual/, not project-based).
